/* eslint-disable react/react-in-jsx-scope */

import { storiesOf } from '@storybook/vue';
import { action } from '@storybook/addon-actions';
import { linkTo } from '@storybook/addon-links';

import MyButton from './MyButton.vue';
import Tabs from '../components/Tabs/Component.vue';
import Welcome from './Welcome.vue';

storiesOf('Welcome', module).add('to Storybook', () => ({
  components: { Welcome },
  template: '<welcome :showApp="action" />',
  methods: { action: linkTo('Button') },
}));

storiesOf('Button', module)
  .add('with text', () => ({
    components: { MyButton },
    template: '<my-button @click="action">Hello Button</my-button>',
    methods: { action: action('clicked') },
  }))
  .add('with JSX', () => ({
    components: { MyButton },
    render() {
      return <my-button onClick={this.action}>With JSX</my-button>;
    },
    methods: { action: linkTo('clicked') },
  }))
  .add('with some emoji', () => ({
    components: { MyButton },
    template:
      '<my-button @click="action"><span role="img" aria-label="so cool">😀 😎 👍 💯</span></my-button>',
    methods: { action: action('clicked') },
  }));


  storiesOf('Components', module)
    .add('Tabs', () => ({
      render: h=>h(Tabs,{
        props: {
          target:'#app',
          title:'Proposals',
          sections:[
            { title:'Proposal Evaluation Process', body:'This is the 1 section body', hidden: false },
            { title:'Incomplete Application Stream', body:'This is the 2 section body', hidden: true },
            { title:'Residential FASTRACK', body:'This is the 3 section body', hidden: true },
            { title:'Commercial Xpress', body:'This is the 4 section body', hidden: true },
            { title:'Section 5', body:'This is the 5 section body', hidden: true },
            { title:'Section 6', body:'This is the 6 section body', hidden: true },
            { title:'Section 7', body:'This is the 7 section body', hidden: true }
          ]
        }

       })

    }))

/* eslint-enable react/react-in-jsx-scope */
