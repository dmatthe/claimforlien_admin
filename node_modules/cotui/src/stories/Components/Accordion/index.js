import '../../config.js';

import { storiesOf } from '@storybook/vue';
import { action } from '@storybook/addon-actions';
import { linkTo } from '@storybook/addon-links';

import { withInfo } from '@storybook/addon-info';
import { withKnobs, text, boolean, number } from '@storybook/addon-knobs';
import { withReadme,doc } from 'storybook-readme';

import { withNotes } from '@storybook/addon-notes';



import Accordion from '../../../components/Accordion';
import StoryReadMe from './Readme.md';
import ReadMe from '../../../components/Accordion/README.md';
let story = storiesOf('Components/Accordion', module);

story.addDecorator(withReadme([StoryReadMe]))
story.add('Overview', doc(ReadMe) )

story.addDecorator(withKnobs);
story.add('Example',()=>{
  const ui = Accordion({
    target:'#js-cotui-app',
    title:'Title',
    toggle: false,
    level: 2,
    sections:[
      { title:'Section 1', body:'This is the 1 section body', expanded: true },
      { title:'Section 2', body:'This is the 2 section body', expanded: false },
      { title:'Section 3', body:'This is the 3 section body', expanded: false },
      { title:'Section 4', body:'This is the 4 section body', expanded: false }
    ]
  })

  console.log(ui)

  return `
    <div>
      <h1>Accordion</h1>
      <div id="js-cotui-app"></div>
    </div>
  `
  

})


