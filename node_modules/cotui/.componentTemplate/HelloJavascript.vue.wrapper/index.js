import $ from 'jquery';

class ___COMPONENTNAME____{
    constructor({target,attr}){
        this.el = target;
        this.attr = attr;

        const $el = document.querySelectorAll(this.el)
        Array.from($el).map((el)=>el.setAttribute('cotui',''))
    }


    init(){
        console.log('Init', this.el)
    }

    render(){
        console.log('Render', this.el, this.attr, $(this.el))
        $(this.el).html('<h1>Hello ___COMPONENTNAME____</h1>')
    }
}


export default ___COMPONENTNAME____
